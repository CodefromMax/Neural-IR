import csv
import json

csvfile = open('Correct_data.csv', 'r')
jsonfile = open('json2.json', 'w')

reader = csv.DictReader(csvfile)
for row in reader:
    json.dump(row, jsonfile)
    jsonfile.write('\n')