import csv
import json

csvfile = open('Data1.csv', 'r')
jsonfile = open('Zhaoqi.json', 'w')

reader = csv.DictReader(csvfile)
for row in reader:
    json.dump(row, jsonfile)
    jsonfile.write('\n')
