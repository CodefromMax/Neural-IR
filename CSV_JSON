import csv
import json

csvfile = open('Haochen.csv', 'r')
jsonfile = open('Haochen.json', 'w')

reader = csv.DictReader(csvfile)
for row in reader:
    json.dump(row, jsonfile)
    jsonfile.write('\n')
