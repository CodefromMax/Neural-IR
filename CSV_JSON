import csv
import json

csvfile = open('Nathan.csv', 'r')
jsonfile = open('Nathan.json', 'w')

reader = csv.DictReader(csvfile)
for row in reader:
    json.dump(row, jsonfile)
    jsonfile.write('\n')
